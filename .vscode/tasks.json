{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build & run",
            "command": "go build -x -o server.exe && ./server.exe",
            "group": {
                "isDefault": true,
                "kind": "build"
            },
            "type": "shell"
        }
    ]
}